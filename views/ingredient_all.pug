extend layout 

block content
	h1= title

	a(href="http://localhost:3000/ingredient/create") Add Ingredient 

	each ingredient of allIngredients 
		p= ingredient
		br
		a(href=`/ingredient/delete/${ingredient._id}`) Delete
		a(href=`/ingredient/update/${ingredient._id}`) Update
		
		
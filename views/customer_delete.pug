extends layout

block content

	form(method="POST") 
		h1 Are you sure you want to delete this customer?

		h3 #{customer.CustomerEmail}

		br
		a(href="/customer/") back
		br
		button(type="submit") Yes 
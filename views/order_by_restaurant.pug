extends layout 

block content
	h1= title
	
	div
		a(href=`/order/${restaurant._id}/add`) add new

	ul
		each order of orders
			li= order

			a(href=`/order/${restaurant._id}/delete`) delete
			br
			a(href=`/order/${restaurant._id}/update`) update

			hr